# No API credentials needed — Pyrogram uses built-in app credentials.
# First run prompts for phone number + OTP code.

# Where bot tokens are stored (default: ~/bot-pool)
BOT_POOL_DIR=/home/pi/bot-pool
