# Mem0 Plugin — Discovery Notes

**Script:** scripts/06-setup-mem0.sh
**Started:** 2026-03-25 19:12 HKT

## Commands Run

```bash
$ openclaw plugins install @mem0/openclaw-mem0
# Downloaded v0.4.0 (newer than Pi5's v0.1.2)
# Auto-configured: plugins.slots.memory = "openclaw-mem0"
# Disabled: memory-core, memory-lancedb

$ cd ~/.openclaw/extensions/openclaw-mem0 && npm rebuild sqlite3
# rebuilt dependencies successfully
```

## Config Applied

In `openclaw.json`, `plugins.entries.openclaw-mem0.config`:
```json
{
  "mode": "open-source",
  "userId": "owner",
  "autoCapture": true,
  "autoRecall": true,
  "enableGraph": false,
  "topK": 5,
  "searchThreshold": 0.3,
  "oss": {
    "embedder": { "provider": "openai", "config": { "model": "text-embedding-3-small", "embeddingDims": 1536 } },
    "vectorStore": { "provider": "qdrant", "config": { "url": "http://localhost:6333", "collectionName": "client-T001-memories" } },
    "llm": { "provider": "deepseek", "config": { "model": "deepseek-chat" } },
    "historyDbPath": "/home/deploy/clawd/mem0-history.db"
  }
}
```

**Key change from Pi5:** LLM switched from `gpt-4.1-nano` (OpenAI) to `deepseek-chat` (DeepSeek). Embedder remains OpenAI.

## Gotchas
- Plugin version jumped from 0.1.2 (Pi5) to 0.4.0 — config format may differ
- Plugin install auto-modifies openclaw.json (creates backup at .bak)
- WARNING about "dangerous code patterns: Environment variable access combined with network send" — expected, the plugin needs API keys to call LLM/embedding APIs
- `npm rebuild sqlite3` needed on x86 (same as ARM, just auto-resolves path)
- DeepSeek as Mem0 LLM provider: using `"provider": "deepseek"` per Mem0 docs

## Verification
```
Gateway logs:
[plugins] openclaw-mem0: registered (mode: open-source, user: owner, graph: false, autoRecall: true, autoCapture: true)
[gateway] openclaw-mem0: initialized (mode: open-source, user: owner, autoRecall: true, autoCapture: true)
```

Gateway healthy with Mem0 loaded. 788MB RAM (up from 678MB without Mem0).

## Time Taken
~3 minutes (install + config + restart)
